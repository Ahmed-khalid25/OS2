name: "Ahmed Khalid"

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: "Testing GitHub actions"
        run: pwd
      - name: "Testing printing"
        run: echo Ahmed
